name: sawitsense_my
description: "SawitSense MY — Open-Source CPO Price Tracker & OER Signal for Malaysian Smallholders"
publish_to: 'none'
version: 0.2.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  go_router: ^13.0.0
  fl_chart: ^0.66.0
  http: ^1.2.0
  intl: ^0.19.0
  url_launcher: ^6.2.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
